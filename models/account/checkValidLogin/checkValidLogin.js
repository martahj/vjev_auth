// @flow

const checkValidLogin = (email: string, password: string): boolean => {
  /*
   * Check credentials against database
   * If matches an entry, return true
   * Else return false
   */
};

export default checkValidLogin;
